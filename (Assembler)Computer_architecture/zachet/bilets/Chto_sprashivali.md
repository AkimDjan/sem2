# Общая информация

Принимают обычно семинаристы - Кадочников и Мишкович. Своему семинаристу сдавать нельзя(но были случаи когда семер забывал что ты у него поэтому все ок сдавал ему) ДОМАШКИ ПОЧТИ НИКАК НЕ ВЛИЯЮТ НА ОЦЕНКУ - МАКСИМУМ С ОТЛ8 НАКИНУТ ОТЛ9-ОТЛ10

Схема сдачи зачета - устно, приходите к преподу и он дает последовательно 3 задачи: IEEE(перевод числа из систем исчислений) + логическая схема(легкая дискретка) + функция на ассемблере = отл 8

Мишкович чисто по задачам, может около них теорку спросить если тупите, Кадочников больше по теорке, обычно никто не валит, без ассемблера будет удос, на пальцах не расшарить, учите ассемблер, дети. Также есть примеры билетов в этом же файлике в jpg и примеры решений, думаю, будет полезно

Отзывы сдачи:

Кадочников:

1) Чуть-чуть про то, как я сдавала:
    1. Сначала просил объяснить в общих чертах перевод числа в IEEE. Особое внимание уделял представлению +-бесконечности и 0, спрашивал, в каких пределах может быть мантиса
    2. Дал задачу про 3 мудрецов, которую он придумал в электричке.
    Есть 3 мудреца, которые говорят либо да, либо нет. У одного мудреца скверный характер и если его ответ не самый популярный, то он начинает драку. Нужно написать булевую функцию, которая будет предсказывать, начнётся драка или нет. Дополнительно просил написать все булевые функции от 1 переменной и посчитать их количество для N переменных
    3. Попросил рассказать про компаратор и нарисовать его схему.
    4. Задача на ассемблер. Передаётся указатель на начало строки, нужно посчитать её длину. Про регистры спрашивает в задаче. Почему именно эти используете

    По 3: в дополнение, в целом очевно, но на всякий случай, он может спросить любую шнягу из первой половины семестра (там где были кодеры, декодеры, сумматоры, компараторы мультиплексоры и прочие)

    Ну и по 4: мне дали написать функцию, в которую передаётся int, выводится 0, если число чётное, 1 — если нечётное.(Есть волшебная операция test $1, регистр, которая вернёт, честный регистр или нет. Вот что и когда(ноль или 1) вернёт, я не помню. Но четность проверяется именно этой штукой ИЛИ ZF == 1 => число четное, иначе нечетное) Деление использовать нельзя вообще, взятие остатка как отдельную операцию, как и функции из любых сторонних библиотек тоже
    
2)  Что делает функция call, Вспомнить, зачем нужен регистр не общего назначения  %rip, Я слышал, он спрашивал у кого-то что-то типо компараторов или мультиплексоров

Мишкович:

3) нормально принимает почти только практику, в задаче ему нужен самый простой метод  только на 3 верном решении принял метод. Нужно знать суффиксы, регистры какие calle  какие caller, и полностью понимать что произойдёт после команд

4) по билетам спрашивает, которые скидывал, они у него так же расписаны в телефоне, а кадочников залупу спрашивает по типу компоратора какого-нибудь и все в таком роде

Вопросы от всех:

5) На код было реализовать при помощи суммы и вычитания деления нацело N на K. У кого-то было остаток от деления N на K, аналогично. Самое главное — знайте про знаки, суффиксы команд. Типо movQ, L, w... Для сравнения (jmp) спрашивал про префиксы. Тоже надо знать все, включая a и b

6) Дана структура Си. В этой структуре есть один элемент типа чар и один элемент типа инт. Написать функцию, которая складывает эти два элемента структуры и возвращает инт. В функцию подается указатель на структуру, главное про смещения не забыть

7) Дается число n и указатель на матрицу интов n x n, надо вернуть наименьший из элементов, лежащих ниже главной диагонали

8) uint32_t short_count( const char* s, uint32_t l_max)
Написать функцию, которая возвращает количество слов, длина которых меньше, чем l_max. Принимает указатель на начало строки, и максимальную длину строки.


